# TODOs

1. Use unsafe rust to fix daemon run async problem
2. Implement kafka_consumer daemon
3. Add Trigger messages as first-class actor messages; allow control flows
4. Add `Custom` nodes that don't define inputs/outputs at runtime
5. Implement dynamic JS node
